---
description: Save a portable handoff of the current work so another session or tool can continue
argument-hint: [stream-tag] ["one-line title"]
allowed-tools: [Bash, Read, Write]
---

Use the `handoff:handoff` skill. Use `$ARGUMENTS` as the stream tag / title if provided.
