schema_version: "1.1"
name: triage-inbox
description: Triage mode reads the whole inbox and decides per capture without writing
tags: [distill, triage]
context:
  scaffold_script: setup.sh
execution:
  max_turns: 40
  timeout_seconds: 600
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
