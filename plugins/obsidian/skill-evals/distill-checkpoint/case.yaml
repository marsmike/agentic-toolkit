schema_version: "1.1"
name: distill-checkpoint
description: Without --auto the skill proposes and stops; nothing is written
tags: [distill, core]
context:
  scaffold_script: setup.sh
execution:
  max_turns: 40
  timeout_seconds: 600
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
