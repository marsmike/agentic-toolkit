schema_version: "1.1"
name: distill-auto
description: An explicit --auto distill of one capture in the example vault
tags: [distill, core]
context:
  scaffold_script: setup.sh
execution:
  max_turns: 80
  timeout_seconds: 900
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
