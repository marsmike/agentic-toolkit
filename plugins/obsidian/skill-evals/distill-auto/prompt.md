The Obsidian vault is at ./vault (it contains the toolkit's example vault). Use the
distill skill to distill the capture vault/01_Capture/Research-Leiden-Community-Detection.md.
This is an explicit --auto run: no checkpoint, go all the way to retiring the capture.
Run the final distill_check with --json and two --ask questions and save its output to
./check.json in the working directory. Then reply with which note now holds the capture's
content (new or an existing note you enriched), which notes you enriched at which level,
and the result of the check.
