# Moved

The cloud routine lives in [`cloud/`](../cloud/README.md): `README.md` (this document),
`pipeline.prompt.md` and `watchdog.prompt.md` (the routines' prompts), `setup.sh` (the
environment's setup script) and `routines.json` (the routines' settings). This stub stays so a
routine whose instruction still names `docs/cloud-routine.md` finds its way: follow
`cloud/pipeline.prompt.md` for the pipeline routine and `cloud/watchdog.prompt.md` for the
watchdog.
