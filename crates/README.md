# crates

Empty until R1 — the first crate is **farsight** (hybrid BM25+vector search),
followed by **gaiafield** (knowledge graph over the vault's wikilinks). Engines
are CLI-in/JSON-out binaries; see `docs/PLAN.md` (Engines) and
`contract/KNOWLEDGE_API.md` for the interface they must implement.
